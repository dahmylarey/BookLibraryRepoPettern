@using Microsoft.EntityFrameworkCore.Metadata.Internal
@model IEnumerable<BookLibraryRepoPetternCore.Book>
@{
    ViewData["Title"] = "Collections Of Books";
}



@*<button asp-action="Create" asp-controller="Book" class="btn btn-danger mb-3">Create New Book</button>*@
@*<form>

    <table class="table table-responsive table-bothered table-hover">

        <tr class="table-active">
            <th>
                @Html.DisplayNameFor(Book => Book.Id)
            </th>
            <th>
                @Html.DisplayNameFor(Book => Book.Title)
            </th>

            <th>
                @Html.DisplayNameFor(Book => Book.Author)
            </th>

            <th>
                @Html.DisplayNameFor(Book => Book.Genre)
            </th>

            <th>
                @Html.DisplayNameFor(Book => Book.Description)
            </th>

            <th>
                @Html.DisplayNameFor(Book => Book.PublishDate)
            </th>
            <th>Action</th>
        </tr>
        <tbody>
            @if (Model != null && Model.Any())
            {
                @foreach (var data in Model)
                {
                    <tr>
                        <td>@data.Id</td>
                        <td>@data.Title</td>
                        <td>@data.Author</td>
                        <td>@data.Genre</td>
                        <td>@data.Description</td>
                        <td>@data.PublishDate</td>
                        <td class="text-center">
                            <div class="btn-group btn-group-sm">
                                <a asp-controller="Book" asp-action="CreateOrEdit" asp-route-id="@data.Id" class="btn btn-success">Edit</a>
                            </div>

                            <div class="btn-group btn-group-sm">
                                <a asp-controller="Book" asp-action="Delete" asp-route-id="@data.Id" class="btn btn-danger">Delete</a>
                            </div>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="7">
                        <div>
                            No Books Available at the moment
                        </div>
                    </td>
                </tr>
            }
        </tbody>

    </table>



    @if (Model != null && Model.Any())
    {
        @foreach (var data in Model)
        {
            <tr>
                <td>@data.Id</td>
                <td>@data.Title</td>
                <td>@data.Author</td>
                <td>@data.Genre</td>
                <td>@data.Description</td>
                <td>@data.PublishDate</td>
                <td class="text-center">
                    <div class="btn-group btn-group-sm">
                        <a asp-controller="Book" asp-action="CreateOrEdit" asp-route-id="@data.Id" class="btn btn-success">Edit</a>
                    </div>

                    <div class="btn-group btn-group-sm">
                        <a asp-controller="Book" asp-action="Delete" asp-route-id="@data.Id" class="btn btn-danger">Delete</a>
                    </div>
                </td>
            </tr>
        }
    }
    else
    {
        <tr>
            <td colspan="7">
                <div>
                    No Books Available at the moment
                </div>
            </td>
        </tr>
    }

</form>*@







<section class="front-product">
    <div class="container-fluid p-0">
        <div class="text-capitalize text-center mb-3 ">
            <h3 class="text-secondary mt-5">@ViewData["Title"]</h3>
        </div><br />

        <div class="row align-items-center justify-content-center gap-3">

            @foreach (var data in Model)
            {
                <div class="col-md-4">
                    <div class="card mb-4" style="max-width: ">
                        <svg class="bd-placeholder card-img-top" width="100%" height="225" src="@Url." alt="Card image cap">

                            <div class="card-body">
                                <h3 class="card-title">@data.Title</h3>
                                <p class="card-text">@data.Description </p>
                                <h4><p class="card-text">@data.PublishDate </p></h4>

                                <div class="btn-group btn-group-sm">
                                    <a asp-controller="Book" asp-action="Delete" asp-route-id="@data.Id" class="btn btn-danger">Delete</a>
                                </div><br /><br />

                                @*<div class="btn-group">
                            <a href="/Book/Index/@data.Id" class="btn btn-sm btn-outline-secondary">View details</a>
                            </div>*@

                                @*<div class="btn-group btn-group-sm">
                            <a asp-controller="Book" asp-action="CreateOrEdit" asp-route-id="@data.Id" class="btn btn-success">Edit</a>
                            </div>*@

                                <small class="text-muted">@data.Author</small>
                             </div>

                    </div>

                </div>
            }

            

        </div>
    </div>
</section>




